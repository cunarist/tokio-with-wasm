[package]
name = "tokio_with_wasm"
version = "0.9.0"
edition = "2021"
license = "MIT"
description = "Mimicking tokio functionalities on web browsers"
repository = "https://github.com/cunarist/tokio-with-wasm"

[package.metadata.docs.rs]
default-target = "wasm32-unknown-unknown"
all-features = true

[features]
# https://github.com/tokio-rs/tokio/blob/master/tokio/Cargo.toml
default = []
full = ["macros", "sync", "time", "rt", "rt-multi-thread"]
# `macros` and `sync` forward to `tokio`, because the glue code
# re-exports those parts of `tokio` even on the web.
# The other features only gate glue modules implemented here;
# their native counterparts come from your own dependency on `tokio`,
# which needs the same features enabled (see the README).
macros = ["tokio/macros"]
sync = ["tokio/sync"]
time = []
rt = []
rt-multi-thread = ["rt"]

[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
wildcard_imports = "deny"

[dependencies]
tokio_with_wasm_proc = "0.9.0"
tokio = "1.x.x"
wasm-bindgen = "0.2.95"
wasm-bindgen-futures = "0.4.45"
js-sys = "0.3.70"
web-sys = { version = "0.3.70", features = [
    'Worker',
    'WorkerOptions',
    'WorkerType',
    'DedicatedWorkerGlobalScope',
    'MessageEvent',
    'ErrorEvent',
    'Blob',
    "BlobPropertyBag",
    'Url',
] }

[dev-dependencies]
# The crate itself, so that the integration tests get its features.
tokio_with_wasm = { path = ".", features = ["full"] }
wasm-bindgen-test = "0.3"
js-sys = "0.3.70"
